# NPM registry config for private DigiStratum packages
# Using public CDN for packages (packages.digistratum.com)
# No auth required for public tarballs
# Note: NPM_TOKEN is only needed if using GitHub Packages registry
